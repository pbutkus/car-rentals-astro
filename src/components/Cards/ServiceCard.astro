---
interface Props {
  service: {
    serviceHeading: string;
    serviceIcon: string;
    serviceDescription: string;
  };
}

const { service } = Astro.props;
---

<div class="p-6 shadow-md rounded-sm bg-white">
  <div class="flex flex-row gap-1 justify-between items-center pb-2">
    <h2 class="text-xl">
      {service.serviceHeading}
    </h2>
    <img
      class="w-12 h-12"
      src={service.serviceIcon}
      alt={service.serviceHeading}
    />
  </div>
  <p class="font-light">{service.serviceDescription}</p>
</div>
